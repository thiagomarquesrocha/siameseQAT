class TrainConfig:
    
    OUTPUT_MODELS = 'models'
    """
        p = preprocessing
        m = model
        e = epochs
    """
    MODEL_NAME = 'model_{}_p_{}_m_{}_e({}).ckpt' # ckpt format does not work